# 画面・デザイン固定 仕様書（Gemini連携用・統合版）

## 1. 本仕様書の目的
本仕様書は、現在作成済みのページ・機能、および今後追加されるすべてのページ（エラーページ含む）に対し、**デザインおよび画面遷移を一切変更せずに開発・修正・拡張を行うこと**を目的とする。

本仕様書は、AI（Gemini）へ実装・修正を依頼する際の**最上位ルール**として適用される。

---

## 2. 適用範囲
以下すべてを本仕様書の適用対象とする。

- 既に作成済みの全ページ
- 今後追加される新規ページ
- Setupページ（初回ログイン時設定）
- Settingsページ（ユーザー設定）
- 正常系画面
- エラーページ（共通エラー画面・個別エラー表現を含む）
- UIコンポーネント（ボタン・枠・入力・カレンダー等）
- 画面遷移・分岐ロジック

---

## 3. 絶対固定事項（最重要ルール）
以下の項目は**理由を問わず変更不可**とする。
変更が必要な場合は、**必ず事前にユーザーの明示的な承認を得ること**。

### 3.1 デザイン固定項目
- レイアウト構造（配置・並び順・余白）
- コンポーネントのサイズ（幅・高さ・padding・margin）
- 色・配色ルール
- フォント種別・サイズ・太さ
- UIコンポーネントの形状・装飾
- アニメーション・表示演出

※「改善」「最適化」「一般的にはこちらが良い」等の理由での変更は禁止

### 3.2 画面遷移固定項目
- 既存の画面遷移フロー
- ページ分岐条件
- 戻る／進む動線

※ UXに影響する遷移変更は一切不可

---

## 4. UIコンポーネント運用ルール

### 4.1 基本方針
- **見た目は完全固定**
- 再利用は「色・文言・状態」のみ変更可能

### 4.2 ボタンに関する規定
- 形状・角の処理・影の有無は固定
- サイズは既存仕様に準拠
- 使用可能な変更点は以下のみ
  - 文言
  - 色（事前定義されたバリエーション内）
  - disabled / loading などの状態

※ 新しい見た目のボタンを作る場合は必ず事前確認

### 4.3 枠・カード・その他UI
- 既存コンポーネントを流用する
- 枠線・装飾・余白の変更は禁止

---

## 5. エラーページ仕様（後付け実装）

### 5.1 基本方針
- エラーページは**後から追加してよい**
- ただし、既存画面のUI・遷移・構造は一切変更しない

### 5.2 構成ルール
- エラーページは原則「共通エラーページ」1種とする
- 表示内容は以下で切り替える
  - エラーコード
  - エラータイプ

### 5.3 表示ルール
- レイアウト・UIは完全固定
- 切り替え可能なのは文言のみ
- ボタン配置・トーンは既存UIに準拠

---

## 6. エラーメッセージ管理

### 6.1 管理方法
- UIとは分離して管理
- エラーコード or タイプをキーとして定義

### 6.2 変更ルール
- 文言追加・修正は可能
- UI・レイアウト変更は禁止
- UXに影響する表現変更は事前確認必須

---

## 7. ページ追加時の規定（将来拡張用）

### 7.1 基本ルール
- 既存デザイン・遷移思想を踏襲する
- 新規ページ追加によって既存ページを変更してはならない

### 7.2 新規ページ作成時の確認事項
- レイアウトは既存構造をベースにする
- 新規UIコンポーネント作成は原則禁止
- 必要な場合は事前に可否確認を行う

---

## 8. 条件付き変更事項（要ユーザー確認）
以下は実施前に**必ず可否を確認**すること。

- 新規エラー種別の追加
- エラー文言のトーン変更
- 状態分岐の追加
- 新規UIパターンの提案

---

## 9. 禁止事項

- デザイン改善・UI最適化の自動提案
- 画面遷移の簡略化・統合
- 見た目を伴うリファクタリング
- ユーザー未確認での仕様解釈変更

---

## 10. 実装時の必須ルール（Gemini向け）

- 本仕様書を最優先で遵守すること
- 判断に迷う場合は必ず質問すること
- 推測での実装は禁止
- 「変えない」ことを最優先で判断すること

---

## 11. 本仕様書の位置づけ
本仕様書は「変更を防ぐための仕様書」である。

開発効率よりも、
**デザイン・画面遷移・体験の一貫性を最優先**とする。

以上。

